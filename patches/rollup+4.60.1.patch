diff --git a/node_modules/rollup/dist/native.js b/node_modules/rollup/dist/native.js
index 962a101..a3f7de6 100644
--- a/node_modules/rollup/dist/native.js
+++ b/node_modules/rollup/dist/native.js
@@ -96,6 +96,12 @@ const requireWithFriendlyError = id => {
 	try {
 		return require(id);
 	} catch (error) {
+		try {
+			return require('@rollup/wasm-node');
+		} catch {
+			// Fall through to the original error handling when the WASM fallback is unavailable.
+		}
+
 		if (
 			platform === 'win32' &&
 			error instanceof Error &&
